# lab -- OPS-START scratch (listen off). Never sticky reindex=1.
# Source: ZeroPerf disposable scratch.
server=1
listen=0
maxconnections=0
rpcuser=@RPCUSER@
rpcpassword=@RPCPASSWORD@
rpcport=@RPCPORT@

# --- Deploy fill-ins ---
# disablewallet=1
