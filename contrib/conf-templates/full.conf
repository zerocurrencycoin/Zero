# full -- listening full node with wallet and txindex (not Insight addressindex).
# Use insight for explorer flags. gen=0.
server=1
rpcuser=@RPCUSER@
rpcpassword=@RPCPASSWORD@
rpcport=@RPCPORT@
txindex=1
gen=0

# --- Deploy fill-ins ---
# addnode=<peer>
# #dbcache=@DBCACHE@
