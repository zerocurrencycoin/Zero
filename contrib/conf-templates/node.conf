# node -- bare zerod + wallet RPC (same keys as prod, no mining solver lines).
server=1
rpcuser=@RPCUSER@
rpcpassword=@RPCPASSWORD@
rpcport=@RPCPORT@
gen=0

# --- Deploy fill-ins ---
# addnode=<peer>
# #txindex=1
# #dbcache=@DBCACHE@
